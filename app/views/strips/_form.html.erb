<%= form_for(strip) do |f| %>
  <% if strip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(strip.errors.count, "error") %> prohibited this strip from being saved:</h2>

      <ul>
      <% strip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_field :image %>
  </div>

  <div class="field">
    <%= f.label :titre %>
    <%= f.text_field :titre %>
  </div>

  <div class="field">
    <%= f.label :strip_number %>
    <%= f.number_field :strip_number %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
